# Showcreator Archive

Old project files (fixture/group/show configurations saved by the user inside
the Show Creator app). These are not application configuration — they are saved
work, kept around as a historical reference / fallback.

The currently-active project file lives at the project root as `config.yaml`.
